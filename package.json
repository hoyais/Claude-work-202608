{
  "name": "bike-weather",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "자전거 날씨 — 주행 중 진행 방향 기준으로 바람 영향도를 보여주는 PWA",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "maplibre-gl": "^4.7.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "playwright": "^1.49.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
