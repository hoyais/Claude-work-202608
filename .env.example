# 목 데이터 모드 — API 키 없이 UI를 확인할 때 true
VITE_USE_MOCK=true

# 서버리스 프록시 주소 (배포 후 workers.dev URL)
VITE_API_BASE=/api

# 지도 타일 (MapTiler 무료 키)
VITE_MAPTILER_KEY=

# --- worker/.dev.vars 에 넣을 값 (클라이언트에 노출되지 않음) ---
# KMA_KEY=기상청_API_허브(apihub.kma.go.kr) authKey
